# Portfolio Project – C++ (CS Course Artifact)

## Project Summary (What problem it solves)
For this course, I chose my **[PROJECT NAME]** project to add to my GitHub portfolio. The goal of this program is to **[1–2 sentences explaining what it does]**. Basically, it takes **[input type: user input / file data / menu choices]** and produces **[output/result]** so the user can **[purpose/benefit]**.

## What I did particularly well
One thing I think I did well was keeping the program organized and readable. I separated logic into functions/classes instead of putting everything in `main()`, and I kept my naming consistent so it’s clear what each piece of code is doing. I also focused on making the output easy for the user to follow, especially when the program uses a menu or repeated prompts.

## Where I could enhance the code (efficiency, security, etc.)
If I went back to improve this project, I would strengthen input validation and error handling. For example, I would make sure the program handles invalid inputs and unexpected file issues more smoothly instead of assuming everything will always be correct. I’d also look for spots where I can reduce repeated work (like extra loops or recalculations) so it runs more efficiently as the program grows.

## Most challenging parts + how I overcame them
The hardest part for me was **[pick one: file handling / data structures / class design / logic for menu options / debugging]**. I overcame it by breaking the problem into smaller steps and testing as I went instead of writing everything at once. I also used debugging output and re-read the requirements/rubric to make sure I wasn’t missing anything. Tools/resources I’m relying on more now are ZyBooks examples, C++ documentation (like cppreference), and using Git/GitHub to track changes and avoid losing progress.

## Transferable skills from this project
The skills that will transfer the most for me are:
- writing structured C++ code (functions/classes)
- debugging and testing in small steps
- working with input/output (console + files, depending on the project)
- thinking about reliability (handling errors, clean user flow)
- keeping code readable and maintainable

## Maintainability, readability, and adaptability
To keep the program maintainable, I used clear naming, consistent formatting, and comments when the logic could get confusing. I also tried to avoid hard-coding values whenever possible so it’s easier to update later. If someone else opened this project, they should be able to follow the structure and make edits without rewriting the whole program.

## How to Run
1. Compile: **[example: g++ main.cpp -o program]**
2. Run: **[example: ./program]**
3. If the program uses a file, make sure **[filename]** is in the same folder as the executable.
